# This file contains names and non-secret placeholders only.
# Copy real values into an untracked local environment file or server secret store.

# Application
APP_ENV=development
APP_URL=http://localhost:3000
APP_TIMEZONE=Asia/Kuala_Lumpur
LOG_LEVEL=info
LOG_REDACTION_ENABLED=true

# Public Supabase configuration (safe to expose only with correctly tested RLS)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Preferred for new Supabase projects. The app falls back to ANON_KEY when absent.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace-with-public-key
# Legacy compatibility. Leave blank when using PUBLISHABLE_KEY.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only Supabase configuration - never expose to browser bundles
SUPABASE_SERVICE_ROLE_KEY=

# STEP 3 intentionally has no OpenAI, Facebook API, or Google Trends credentials.

# ===== STEP 4: OpenAI (server-side only — never NEXT_PUBLIC_, never logged, never committed) =====
OPENAI_API_KEY=
OPENAI_MODEL_QUICK=
OPENAI_MODEL_STANDARD=
OPENAI_MODEL_DEEP=
OPENAI_MAX_RETRIES=2
OPENAI_REQUEST_TIMEOUT_MS=120000
# Optional price config per model (USD per 1M tokens, "input,output"), e.g. OPENAI_PRICE_GPT_5_5=2.5,10
# Without a price entry the system shows token estimates only and marks cost unavailable.
RUN_OPENAI_SMOKE_TEST=false
