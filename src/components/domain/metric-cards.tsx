import { CurrencyDollar, Heartbeat, Pulse } from "@phosphor-icons/react/ssr";
import { Card, CardContent } from "@/components/ui/card";
import { StatusBadge } from "@/components/ui/status-badge";

export function CostUsageCard(){return <Card><CardContent><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-brand-50 text-brand-600"><CurrencyDollar/></span><StatusBadge>尚未连接</StatusBadge></div><p className="mt-4 text-sm font-semibold">AI Cost Usage</p><p className="mt-1 text-2xl font-bold text-ink">RM 0.00</p><p className="mt-2 text-xs text-slate-500">没有调用任何付费 AI 服务。</p></CardContent></Card>}
export function SystemHealthCard(){return <Card><CardContent><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-brand-50 text-brand-600"><Heartbeat/></span><StatusBadge tone="warning">需要设置</StatusBadge></div><p className="mt-4 text-sm font-semibold">Source Health</p><p className="mt-1 text-sm text-slate-500">还没有真实来源可检查。</p><div className="mt-4 h-1.5 rounded-full bg-slate-100"><div className="h-full w-0 rounded-full bg-brand-500"/></div></CardContent></Card>}
export function OperationalCard({title,status="暂不可用",description}:{title:string;status?:string;description:string}){return <Card><CardContent><div className="flex items-center justify-between"><Pulse className="text-brand-600" size={22}/><StatusBadge>{status}</StatusBadge></div><p className="mt-4 text-sm font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{description}</p></CardContent></Card>}
